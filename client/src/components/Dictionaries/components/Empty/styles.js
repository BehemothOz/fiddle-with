import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
    root: {
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center'
    }
})